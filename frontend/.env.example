# Local development defaults to http://localhost:8001 when this is empty.
# In production, set this to the public backend origin, for example:
# REACT_APP_BACKEND_URL=https://krishigears-api.onrender.com
REACT_APP_BACKEND_URL=
